# ==============================================
# MARITIME DASHBOARD - BACKEND CONFIGURATION
# ==============================================

# Server Configuration
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/maritime-dashboard
# For MongoDB Atlas, use:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/maritime-dashboard?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=30d

# ==============================================
# INSTRUCTIONS:
# ==============================================
# 1. Copy this file to .env
# 2. Replace MONGO_URI with your actual MongoDB connection string
# 3. Generate a secure JWT_SECRET (use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# 4. Never commit .env to version control
# ==============================================
